# TODO — alignement horizontal des vidéos

- [ ] Replacer la section vidéos existante (`Blog.tsx` + CSS associé) pour que les cartes soient alignées au même niveau.
- [ ] Corriger le style `.video-card__video` pour éviter les effets liés à `height:auto`/`object-fit` sur différentes tailles.
- [ ] S’assurer que `.video-card` garde une hauteur stable et que l’élément titre n’introduit pas de décalage visuel.
- [ ] Ajouter `align-items: stretch` et/ou `align-items:center` au bon conteneur `.video-row`.
- [ ] Mettre à jour `Blog.css` (styles vidéos) et vérifier sur desktop + mobile.

